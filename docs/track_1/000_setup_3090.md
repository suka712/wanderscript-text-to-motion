# SETUP_3090_jul9 — bring the 3090 to parity

Branch: `track1-grounding` (create from a clean, committed `main`).
Read CLAUDE.md and CONTEXT_HANDOFF.md first.

This box is fresh. It must reach the same working state as the 4090 before the grounding
probe (`grounding_probe.md`) can run. This spec writes no model code — it is environment +
data parity, ending in a smoke test. **Gate: do not start the probe until the smoke test
reproduces a known number.**

## 0. Branch
- On `main`, confirm the current validated state is committed and clean.
- `git checkout -b track1-grounding`. All 3090 work commits here.

## 1. Repos
- Clone the `wander` repo (contains motion_features.py, humanise_join.py, vqvae_loader.py,
  verify scripts, CLAUDE.md).
- Clone / copy the sibling T2M-GPT repo that `wander` points at (the 4090 has it at
  `/home/user/Khiem-ssh/T2M-GPT/`). Keep the same relative arrangement `wander` expects.

## 2. Checkpoints
- T2M-GPT pretrained **VQ-VAE** checkpoint (`net_best_fid.pth`) — required (frozen tokenizer).
- T2M-GPT pretrained **transformer** checkpoint — required (starting point for conditioning).
- Record paths + source. Confirm they load with strict=True.

## 3. Data — the item most likely to eat time
The two-track preprocessed data lives on the 4090 disk (`/media/user/2tb/motion_data/`):
HUMANISE (canonicalized 263 + world-frame trajectories), HumanML3D, ScanNet meshes.
- Decide transfer path: shared/mounted storage if available, else rsync over LAN.
- **Verify the 3090 can actually read the data before anything else** — a stalled multi-GB
  copy is the classic silent week-killer. Check total size, start the transfer early,
  confirm completion (checksums or file counts), don't assume.
- Both tracks need HUMANISE tokens; the 3090 will extract its own from the FROZEN VQ-VAE
  (see probe spec) — it needs the 263 canonicalized track + the world-frame track.

## 4. Environment
- Install torch + CUDA matching the **3090** (do not assume it mirrors the 4090's build).
- The numpy-compat shim and any pinned deps from `wander`.
- Rebuild the env from the repo's requirements; verify `import torch; torch.cuda.is_available()`.

## 5. Smoke test — the gate
- Run one existing verify script that produces a known number — e.g. the reconstruction
  canary — on the 3090.
- **Pass = it reproduces a known value** (e.g. HUMANISE walk ~47.9mm reconstruction).
- If the number matches: 3090 is at parity, proceed to `grounding_probe.md`.
- If it doesn't: environment/data/checkpoint mismatch — STOP and diagnose. Do not build
  the probe on an unverified box.

## Deliverable
Commit setup scripts + a short `RESULTS.md` note in `track1_grounding/` recording: data
path on the 3090, checkpoint paths, env versions, and the smoke-test number vs expected.
End with a clear PASS/FAIL on parity.